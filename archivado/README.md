# Archivado

`servidor-backend/` fue un backend Express + PostgreSQL + JWT que el ERP **no usa**:
la aplicacion habla directamente con Firestore. Se conserva aqui como referencia
historica, no como codigo activo.

Sus credenciales (DATABASE_URL y JWT_SECRET) fueron rotadas; las que aparecen en el
historial de git anterior al commit 14be3e6 ya no son validas.
